{
  "name": "ase-agent-stock-exchange",
  "version": "1.0.0",
  "description": "ASE - Agent Stock Exchange backend for waitlist management and AI agent marketplace",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "pages:dev": "wrangler pages dev --local",
    "pages:deploy": "wrangler pages publish",
    "build": "echo 'No build needed for static assets'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "dotenv": "^16.0.3",
    "nodemailer": "^6.9.1",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
